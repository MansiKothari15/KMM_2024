<?xml version="1.0" encoding="utf-8"?>
<resources>
<!--    <style name="Theme.SplashScreenAPI" parent="android:Theme.Material.Light.NoActionBar" />-->

    <!-- Base application theme. -->
    <style name="Theme.KMMApp" parent="android:Theme.Material.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="android:colorPrimary">@color/primaryColor</item>
        <!-- Secondary brand color. -->
        <item name="android:colorSecondary">@color/secondaryColor</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/primaryColor</item>

        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.SplashScreen.MySplash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/primaryDarkColor</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
<!--        <item name="postSplashScreenTheme">@style/Theme.SplashScreen</item>-->
    </style>

</resources>